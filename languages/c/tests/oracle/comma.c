int putchar(int c);
int two(int a, int b) { return a * 10 + b; }
int main() {
    int i; int j; int n = 0; int arr[5]; int v = (1, 2); int k;
    for (i = 0, j = 4; i < j; i++, j--) n = n + j - i;
    k = (putchar('x'), putchar('y'), 5);
    putchar(10);
    return n * 100 + two((i, 3), j) + v + k + sizeof(arr) + sizeof((0, arr)) * 2;
}
