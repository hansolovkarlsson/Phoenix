program Booleans(output);
var
  p, q : boolean;
begin
  p := true; q := false;
  writeln(p);
  writeln(q);
  writeln(p and q);
  writeln(p or q);
  writeln(not p);
  writeln(1 < 2);
  writeln(2 = 3)
end.
