(* `letter` is a helper and is not marked one, so it will be returned as a
 * token of its own. *)
letter = "a" .. "z" .
name = letter { letter } .
space = " " { " " } .
%skip space .
%syntax .
%start line .
line = name .
