(* Two modules that import each other. A file is read once however many times
 * it is named, which is what stops this reading forever -- so a cycle is not
 * an error to report, it simply terminates. *)
%import "circular-b.phx"
%syntax
%start line
line = integer .
